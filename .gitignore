# Secrets — never commit. The registry holds public descriptors only.
.env
.env.*
*.pem
*.key
*_token*
*secret*
credentials*.json
operator-tokens.*          # operator-side {agent-id: token} map for scripts/sync_to_trinity.py

# Python
__pycache__/
*.pyc
.venv/
venv/
.pytest_cache/

# OS / editor
.DS_Store
*.swp
.idea/
.vscode/
